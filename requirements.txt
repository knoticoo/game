# System Requirements for King's Choice Game Server
# Install these system packages before running the game

# Node.js and npm (install via NodeSource repository)
# curl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash -
# sudo apt-get install nodejs -y

# Alternative: Install via package manager
nodejs>=18.0.0
npm>=8.0.0

# System packages that might be needed
curl
git

# For development (optional)
build-essential
python3
python3-pip

# For production deployment (optional)
nginx
certbot